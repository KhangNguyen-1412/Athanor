import type { SecretProfile } from '../../types/athanor';

export interface HeroDossierItem {
  birthplace: string;
  birthday: string;
  height: string;
  secretProfile: SecretProfile;
  specialLore: string;
}
